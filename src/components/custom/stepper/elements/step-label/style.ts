import styled from '@emotion/styled';
import { StepLabel } from '@mui/material';

export const StepLabelMain = styled(StepLabel)`
  display: grid;
  place-items: center;

  .MuiStepLabel-label {
    font-family: 'Inter', sans-serif;
  }
`;
